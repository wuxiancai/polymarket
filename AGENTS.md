# 项目规则

- 默认使用中文沟通。
- 本项目必须作为 git 工程维护。
- 以后每次修改项目文件，都必须在验证后提交一个 git commit。
- 不提交 `.venv/`、`data/*.sqlite3`、缓存文件或其他运行产物。
- 首页为 Polymarket 真实交易系统；模拟系统独立保留在 `/simulation`，不接钱包、不真实下单。
- 真实下单只允许用户显式确认后执行，不自动下单。
- 不把 Polymarket 私钥或 Relayer API key 写入数据库；只允许保存在进程内存或环境变量。
- 任务完成或中断前，更新 `docs/HANDOFF.md` 中的重要上下文。
