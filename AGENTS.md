# 项目规则

- 默认使用中文沟通。
- 本项目必须作为 git 工程维护。
- 以后每次修改项目文件，都必须在验证后提交一个 git commit。
- 不提交 `.venv/`、`data/*.sqlite3`、缓存文件或其他运行产物。
- 系统默认只做 Polymarket 只读扫描和纸面模拟交易，不接钱包、不真实下单。
- 任务完成或中断前，更新 `docs/HANDOFF.md` 中的重要上下文。
